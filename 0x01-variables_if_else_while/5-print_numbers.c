#include <stdio.h>

/**
 * main - A program that print all single digit number of base 10
 * Return: 0 is return to the OS if program is successful
 */

int main(void)
{
	int i = 0;

	while (i < 10)
	{
	printf("%d", i);
	i++;
	}

	printf("\n");

	return (0);
}
